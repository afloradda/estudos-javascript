// Como declarar variáveis em JavaScript:
// Variáveis são espaços criados para guardar valores.

let variavel = 10;
variavel = 20;
/* let são variáveis as quais o valor armazenado podem ser mutáveis.
   as 'const' são variáveis do tipo constante, uma vez o valor declarado não poderá
   ser mudado.
 */
const pi = 3.14;

console.log("print em JavaScript");

/* Operadores mátematicos seguem sendo:
   + (adição) | - (subtração) | * (multiplicação) | / (divisão)

   Tal qual o Operador de Atribuição: = (igual)
*/

/* No terminal usamos nosso arquivo JavaScript após o comando node, dessa forma:
   node aula.js
 */